@model SistemaSuscripciones.Entities.Plan

@{
    ViewData["Title"] = "Crear Plan";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">Registrar Nuevo Plan</h5>
                </div>
                <div class="card-body">
                    <form asp-action="Crear" method="post">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Nombre del Plan</label>
                            <input asp-for="Nombre" class="form-control" placeholder="Ej. Plan Semestral" required />
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label class="form-label fw-bold">Precio ($)</label>
                                <input asp-for="Precio" class="form-control" type="number" step="0.01" min="0.01" placeholder="Ej. 75.00" required />
                            </div>
                            <div class="col-md-6">
                                <label class="form-label fw-bold">Duración (Meses)</label>
                                <input asp-for="DuracionMeses" class="form-control" type="number" min="1" placeholder="Ej. 6" required />
                            </div>
                        </div>

                        <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
                            <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
                            <button type="submit" class="btn btn-primary">Guardar Plan</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
